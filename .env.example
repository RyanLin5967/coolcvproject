# Copy to .env.local and fill in locally. Never paste credentials into chat.
# No application loader exists yet; execution setup will load this explicitly.
ROBOFLOW_API_KEY=
ROBOFLOW_WORKSPACE=

# Modal authentication will use `modal setup` and its local credential store.
# No Hugging Face, W&B, or Exa credential is needed by the delivered project.
